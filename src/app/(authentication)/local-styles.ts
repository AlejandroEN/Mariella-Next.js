"use client";

import styled from "styled-components";

export const Title = styled.label`
	color: #000;
	font-family: var(--motiva-sans-bold);
	font-size: 40px;
	font-style: normal;
	line-height: normal;
`;